NA-0004
Unify Memory System
Refactoring
MEDIUM

Three memory systems coexist: SQLite Memory (old), FileMemory (canonical), and
HybridMemoryManager (new). They have overlapping responsibilities and no clear
migration path. Need to unify into a single coherent system.

## AGENT
Unassigned

## DEPENDENCIES
- (none)

## PROJECT
NexusAgent

## FILES TO MODIFY
- src/nexusagent/memory/memory.py
- src/nexusagent/memory/memory_files.py
- src/nexusagent/memory/index/embeddings.py
- src/nexusagent/memory/index/index.py

## FILES TO CREATE
- src/nexusagent/memory/README.md (architecture doc)

## ACCEPTANCE CRITERIA
- [ ] Single unified memory interface (one class, one API)
- [ ] Backward compatible with existing SQLite data
- [ ] Embedding fallback chain preserved (Gemini → local → hash)
- [ ] All 599 tests still pass
- [ ] Memory architecture doc created

## NOTES
This is a design-heavy task. Write the architecture doc FIRST before touching code.
The key insight: FileMemory should be the primary, SQLite the cache/vector index.
