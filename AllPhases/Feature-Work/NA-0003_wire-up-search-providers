NA-0003
Wire Up Search Providers
Feature-Work
MEDIUM

The TUI has a `/search` command but no actual search providers are wired in.
The code references search_web but the tool is not registered in the agent's tool list.

## AGENT
Unassigned

## DEPENDENCIES
- (none)

## PROJECT
NexusAgent

## FILES TO MODIFY
- src/nexusagent/tools/registry/core.py (or wherever tools are registered)
- src/nexusagent/interfaces/tui.py (search command handler)

## FILES TO CREATE
- src/nexusagent/tools/search.py (if not existing)

## ACCEPTANCE CRITERIA
- [ ] `/search <query>` in the TUI returns actual web results
- [ ] Results are displayed in the chat view, not just logged
- [ ] Graceful fallback if search provider is unavailable
- [ ] Existing tests still pass

## NOTES
Check if search_web exists as a tool but just isn't registered, or if it needs
to be built from scratch using web_search via the LLM provider.
